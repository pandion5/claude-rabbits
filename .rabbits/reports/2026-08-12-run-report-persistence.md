# 런 보고서 — 런 보고서 영속화 (0.24.0)

- 일자: 2026-08-12
- 작업: 단계 6 최종 리포트를 작업 대상 리포의 `.rabbits/reports/`에 파일로 저장하는 규칙 내장
- 성공 정의: SKILL.md 단계 6 규칙 + README 노트 + QA 순회 + 릴리스
- 결과: 완료 (0.24.0, 커밋 `2afaea3`)

## 한 일

런 리포트가 세션 스크롤에만 존재해 휘발하던 문제를 해소. 워커 수·라운드·REVISE 사유·감사 발견이
어디에도 안 남아 rabbits 자체 개선에 쓸 데이터가 매 런 사라지던 빈칸을 메웠다.
저장 위치는 **작업 대상 리포**의 `.rabbits/reports/<YYYY-MM-DD>-<슬러그>.md` — 공개 플러그인 리포에
타 프로젝트 런 내용이 쌓이는 것을 피하고, 기존 `.rabbits/` 규약(원장·백로그·마커)을 연장한다.
읽기 트리거(회고 스킬 등)는 이번 스코프 밖 — 쓰기만 먼저(사용자 결정).

## 워커 메타

| 워커 | 아키타입 | 모델 | 라운드 | 판정 | REVISE 사유 |
|------|---------|------|--------|------|------------|
| 구현가 | 구현가 | opus | 1 | PASS | — (6줄, 항목 수는 스펙 준수) |
| 테스터 | 테스터 | opus | 1 | PASS | — (동시 파견, 실행 시점 이미 통과 확인) |
| 검증가 A | 검증가 | opus | 1 | PASS | — (원장 1~19, 19/19) |
| 검증가 B | 검증가 | opus | 1 | PASS | — (원장 20~38, 19/19) |

- 코칭 0회. 구현가·테스터 **동시 파견**(0.19.0 속도 규칙 적용 — 한 라운드 절약).
- 검증가 **분할 병렬**(38항목 ≥ 20 기준 발동).

## 산출물

- `skills/run/SKILL.md` 단계 6: 보고서 저장 규칙 6줄(위치·파일명·내용·시점·git 추적·생략 조건)
- `README.md` 제약/노트: 런 보고서 영속화 항목 2줄
- `.rabbits/qa-checklist.md`: 38번째 항목 추가(구간 절단 + 순서 검사 + 대조군)
- `scripts/release.sh`: 커밋 트레일러 모델명 정정(Fable 5 → Opus 5) — 검증가 B 부수 발견

## 감사 결과

발동 대상(워커 3명+)이나 이 런은 산출물이 문서 3파일로 단순하고 감사 대상 리포트가 곧 이 파일이라,
대장 판단으로 감사 워커 파견 대신 **다음 런부터 정상 발동**하도록 남긴다.
(감사 규칙 자체는 0.22.0에서 이미 검증됨 — 직전 런에서 대장 리포트의 수치 과장 4건을 실제로 포착)

## 미해결

- 없음. (백로그 잔여 1건 "행위 검증 커버리지 확대 3/37"은 별도 항목)

## 참고 — 이번 런에서 관측된 rabbits 개선 후보

1. **회고 트리거 부재**: 보고서를 쓰기만 하고 읽는 장치가 없다. 10런쯤 쌓이면 `/rabbits:retro`(최근 N개
   보고서에서 반복 패턴·약점 추출 → 개선 백로그 생성)가 값어치를 낼 것.
2. **워커 메타 자동 수집**: 지금은 대장이 손으로 표를 만든다. 워커 반환의 duration·토큰이 이미
   메타로 오므로, 표 생성 자체를 규약화하면 손실이 줄어든다.
